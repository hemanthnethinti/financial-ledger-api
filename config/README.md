# Configuration

This directory is reserved for environment-specific configuration
(e.g., database credentials, ports, feature flags).

For this assignment, configuration is kept simple and defined
directly inside the application code for clarity and ease of evaluation.
